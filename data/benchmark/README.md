# Benchmark data entrypoint

Bundled sample corpus for Standard RAG baseline runs lives at:

`data/benchmark_corpus/financial_policy/`

See `docs/benchmarks/BENCHMARK.md` for the locked contract (`financial_policy_v1`).
