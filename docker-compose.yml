services:
  train:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./data:/app/data
      - ./experiments:/app/experiments
    command: ["python", "scripts/train.py", "--config", "configs/risk_training.yaml"]
