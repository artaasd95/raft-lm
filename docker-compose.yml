# Risk-Aware Fine-Tuning — training-only compose
services:
  train:
    build: .
    volumes:
      - ./data:/app/data
      - ./experiments:/app/experiments
      - ./checkpoints:/app/checkpoints
    command: ["python", "scripts/train.py", "--config", "configs/methods/ppo_env.yaml"]
